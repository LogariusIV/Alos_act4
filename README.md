# Alos_act4

## RUN THE SYSTEM

We can easily run the whole with only a single command:

```
docker-compose up
```

you can check the current working containers:
```
docker ps
```

And Docker images:
```
docker images
```
